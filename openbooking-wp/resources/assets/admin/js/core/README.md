# Admin Core Modules

Shared admin frontend modules live here: API client, notices, DOM helpers, tabs, and form helpers.

The current admin behavior is preserved through `../legacy-admin.js`. New code should be extracted here gradually and imported from `../app.js`.
